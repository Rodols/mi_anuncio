import { Time } from '@angular/common';

export interface Ad {
  id?: string;
  title?: string;
  categorie?: string;
  description?: string;
  image?: string;
  email?: string;
  direction?: string;
  phone?: number;
  web?: string;
  dateCreated?: number;
}
